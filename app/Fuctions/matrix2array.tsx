// Fuction to generate a array from a matrix
const matrix2array = (matrix: number[][]) => {
    console.log("flattening done", matrix.flat())

    return matrix.flat();
    

}

export default matrix2array 